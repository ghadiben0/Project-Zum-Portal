# version: "2"

# build:
#   os: "ubuntu-22.04"
#   tools:
#     python: "3.10"

# python:
#   install:
#     - requirements: docs/requirements.txt

# sphinx:
  configuration: docs/source/conf.py

version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt
    
sphinx:
  configuration: docs/source/conf.py

versions:
  only:
    - stable

html:
  theme_options:
    version_selector: false
